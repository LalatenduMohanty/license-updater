pandas
tabulate

# Note: dnf and packaging are only needed for the DNF API version
# For the current subprocess version, these are not required:
# dnf
# packaging

# Testing dependencies
pytest>=7.0.0
pytest-mock>=3.10.0 